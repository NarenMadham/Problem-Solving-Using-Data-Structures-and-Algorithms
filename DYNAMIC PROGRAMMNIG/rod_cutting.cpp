#include<bits/stdc++.h>
using namespace std;

int main(){
    int n;
    cin>>n;
    int l[n];
    int c[n];
    for(int i=0;i<n;i++){
        cin>l[i];
    }
    for(int i=0;i<n;i++){
        cin>>c[i];
    }
    int k;
    cin>>k;
    int dp[n][k+1];
    memset(dp, 0, sizeof(dp));
    for(int i=0;i<n;i++){
        if()
    }
}